export const auth_config = {

    secret: "skdjfkdjsgldsfkingdfsg",
    jwtExpiration: 3600,           // ? 1 hour
    jwtRefreshExpiration: 86400,   // ? 24 hours

    // Rounds password hash
    ROUNDS: 3

}